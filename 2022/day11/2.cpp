// use lcm of the divisibility tests, and apply modulo lcm to truncate the integer values so that they don't overflow
// could also use BigInt if using javascript

#include <bits/stdc++.h>
using namespace std;
#define ll long long
